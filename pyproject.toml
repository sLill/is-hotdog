[project]
name = "is-hotdog"
version = "0.2.0"
description = "Check whether the contents of a string are 'hotdog'"
readme = "README.md"
authors = [
    { name = "sLill" }
]
requires-python = ">=3.14"
dependencies = []

[build-system]
requires = ["uv_build>=0.11.16,<0.12.0"]
build-backend = "uv_build"
